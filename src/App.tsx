import Home from './views/pages/Home';
//This file serves as the root component of the application, 
//and its only responsibility is to delegate the main view to Home.

export default function App() {
  return <Home />;
}
